class Solution {
public:
    int mirrorDistance(int n) {
       int rev =0;
        int r=0;
        int og = n;
        while(n>0){
            r = n%10;
            rev = (rev*10)+r;
            n = n/10;
            }
            
        return abs(og - rev);
    }
};
