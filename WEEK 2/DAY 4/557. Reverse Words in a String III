class Solution {
public:
    string reverseWords(string s) {
        string word = "";
        string rev = "";

        for (char c : s) {
            if (c != ' ') {
                word += c;
            }
            else {
                reverse(word.begin(), word.end());
                rev += word + " ";
                word = "";
            }
        }

        
        reverse(word.begin(), word.end());
        rev += word;

        return rev;
    }
};
